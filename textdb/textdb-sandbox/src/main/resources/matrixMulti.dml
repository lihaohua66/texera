A = matrix(8, rows=2,cols=2);
#B = matrix(3, rows=2,cols=2);
#C = 10;
#D = A %*% B + C * 2.1;
#print( "output : D[2,1]:" + as.scalar(D[2,1]));
predicted_h = rowIndexMax(A);

write(predicted_h, "./tmp", format="text");
